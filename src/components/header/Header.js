import React from 'react';
import { Logo } from '../../styles/Logo';
import { HeaderStyle } from './HeaderStyle';

export const Header = () => {
  return (
    <HeaderStyle>
      <Logo></Logo>
    </HeaderStyle>
  );
};
